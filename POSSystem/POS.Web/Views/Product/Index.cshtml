@model IEnumerable<POS.Web.Models.Entities.Product>
@using POS.Web.Models.Entities

@{
    ViewData["Title"] = "จัดการสินค้า";
}

<div class="container-fluid">
        <!-- ส่วนค้นหาและกรอง -->
    <div class="card mb-3">
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-8">
                    <div class="input-group">
                        @* <span class="input-group-text">🔍</span> *@
                        <input type="text" class="form-control" id="searchInput" 
                               placeholder="ค้นหาสินค้า..." oninput="filterProducts()">
                    </div>
                </div>
                <div class="col-md-4">
                    <!-- ส่วนของ dropdown หมวดหมู่ -->
                    <select class="form-select" id="categoryFilter">
                        <option value="">ทั้งหมด</option>
                        @foreach (var category in ViewBag.Categories)
                        {
                            <option value="@category.Id">@category.Name</option>
                        }
                    </select>
                </div>
            </div>
        </div>
    </div>

    <!-- ส่วนหัวตาราง -->
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>📦 จัดการสินค้า</h2>
        <div class="btn-group">
            <button class="btn btn-success me-2" onclick="showCategoryModal()">
                📁 จัดการหมวดหมู่
            </button>
            <button class="btn btn-primary" onclick="showAddModal()">
                ➕ เพิ่มสินค้า
            </button>
            <button class="btn btn-outline-success me-2" onclick="exportTemplate()">
            📥 ดาวน์โหลด
            </button>
            <button class="btn btn-outline-primary me-2" onclick="document.getElementById('importFile').click()">
            📤 นำเข้าสินค้า
            </button>
        </div>
        <input type="file" id="importFile" accept=".xlsx" style="display: none;" onchange="importExcel(this)" />
    </div>

    <!-- ตารางสินค้า -->
    <div class="card">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th>หมวดหมู่</th>
                        <th>บาร์โค้ด</th>
                        <th>ชื่อสินค้า</th>
                        <th class="text-end">ราคา</th>
                        <th class="text-center">คงเหลือ</th>
                        <th class="text-center">สถานะ</th>
                        <th>รูปภาพ</th>
                        <th class="text-end">จัดการ</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td data-category-id="@item.CategoryId">@item.Category?.Name</td>
                            <td>@item.Barcode</td>
                            <td>@item.Name</td>
                            <td class="text-end">@item.Price.ToString("N2")</td>
                            <td class="text-center">
                                <span class="badge @(item.Stock > 10 ? "bg-success" : "bg-danger")">
                                    @item.Stock
                                </span>
                            </td>
                            <td class="text-center">
                                <div class="form-check form-switch d-flex justify-content-center">
                                    <input class="form-check-input" type="checkbox" 
                                        onchange="toggleStatus(@item.Id, this)"
                                        @(item.Status ? "checked" : "")>
                                </div>
                            </td>
                            <td>
                                @if (!string.IsNullOrEmpty(item.ImageBase64))
                                {
                                    <img src="data:@item.ImageType;base64,@item.ImageBase64" 
                                        alt="@item.Name" class="product-image" />
                                }
                            </td>
                            <td class="text-end">
                                <button class="btn btn-sm btn-info" onclick="showEditModal(@item.Id)">
                                แก้ไข
                                </button>
                                <button class="btn btn-sm btn-danger" onclick="deleteProduct(@item.Id)">
                                ลบ
                                </button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- Modal จัดการหมวดหมู่ -->
<div class="modal fade" id="categoryModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">จัดการหมวดหมู่</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <!-- ฟอร์มเพิ่มหมวดหมู่ -->
                <form id="categoryForm" class="mb-4">
                    <div class="input-group">
                        <input type="text" class="form-control" id="newCategoryName" placeholder="ชื่อหมวดหมู่ใหม่">
                        <button type="button" class="btn btn-success" onclick="addCategory()">
                            เพิ่ม
                        </button>
                    </div>
                </form>

                <!-- Switch สลับการแสดงผล -->
                <div class="d-flex justify-content-end mb-3">
                    <div class="form-check form-switch">
                        <input class="form-check-input" type="checkbox" id="showDeletedSwitch"
                            onchange="toggleCategoryView()">
                        <label class="form-check-label" for="showDeletedSwitch">
                            แสดงรายการที่ถูกลบ
                        </label>
                    </div>
                </div>

                <!-- ตารางแสดงรายการหมวดหมู่ -->
                <div class="table-responsive">
                    <table class="table table-hover" id="categoryTable">
                        <thead class="table-light">
                            <tr>
                                <th>ชื่อหมวดหมู่</th>
                                <th class="text-center">จำนวนรายการ</th>
                                <th class="text-end">จัดการ</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var category in (List<Category>)ViewBag.Categories)
                            {
                                var productCount = ((Dictionary<int, int>)ViewBag.ProductCounts)[category.Id];

                                <tr data-category-id="@category.Id" data-active="true">
                                    <td>
                                        <span class="category-name">@category.Name</span>
                                        <input type="text" class="form-control category-edit d-none" value="@category.Name">
                                    </td>
                                    <td class="text-center">
                                        @productCount รายการ
                                    </td>
                                    <td class="text-end">
                                        <button class="btn btn-sm btn-warning edit-btn"
                                            onclick="startEditCategory(this, @category.Id)">
                                            ✏️ แก้ไข
                                        </button>
                                        <button class="btn btn-sm btn-success save-btn d-none"
                                            onclick="saveCategory(this, @category.Id)">
                                            💾 บันทึก
                                        </button>
                                        <button class="btn btn-sm btn-danger" onclick="deleteCategory(@category.Id)">
                                            🗑️ ลบ
                                        </button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal Products -->
<div class="modal fade" id="productModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalTitle">เพิ่มสินค้า</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form id="productForm" novalidate>
                    <input type="hidden" id="productId" />
                    
                    <div class="mb-3">
                        <label for="categoryId" class="form-label">หมวดหมู่ <span class="text-danger">*</span></label>
                        <select class="form-select" id="categoryId" required>
                            <option value="">เลือกหมวดหมู่...</option>
                            @foreach (var category in ViewBag.Categories)
                            {
                                <option value="@category.Id">@category.Name</option>
                            }
                        </select>
                        <div class="invalid-feedback">กรุณาเลือกหมวดหมู่</div>
                    </div>

                    <div class="mb-3">
                        <label for="barcode" class="form-label">บาร์โค้ด <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" id="barcode" required>
                        <div class="invalid-feedback">กรุณาระบุบาร์โค้ด</div>
                    </div>

                    <div class="mb-3">
                        <label for="name" class="form-label">ชื่อสินค้า <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" id="name" required>
                        <div class="invalid-feedback">กรุณาระบุชื่อสินค้า</div>
                    </div>

                    <div class="mb-3">
                        <label for="price" class="form-label">ราคา <span class="text-danger">*</span></label>
                        <input type="number" class="form-control" id="price" min="0" step="0.01" required>
                        <div class="invalid-feedback">กรุณาระบุราคา</div>
                    </div>

                    <div class="mb-3">
                        <label for="stock" class="form-label">จำนวน <span class="text-danger">*</span></label>
                        <input type="number" class="form-control" id="stock" min="0" required>
                        <div class="invalid-feedback">กรุณาระบุจำนวน</div>
                    </div>

                    <div class="mb-3">
                        <label for="image" class="form-label">รูปภาพ</label>
                        <input type="file" class="form-control" id="image" accept="image/*">
                        <div id="imagePreview" class="mt-2"></div>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">ยกเลิก</button>
                <button type="button" class="btn btn-primary" onclick="saveProduct()">บันทึก</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {

    @* // script Categories *@
    <script>
        let categoryModal;
        let isShowingDeleted = false;

        document.addEventListener('DOMContentLoaded', function () {
            categoryModal = new bootstrap.Modal(document.getElementById('categoryModal'));
        });

        function toggleCategoryView() {
            const showDeleted = document.getElementById('showDeletedSwitch').checked;
            loadCategories(showDeleted);
        }

        // เพิ่มฟังก์ชันสำหรับการแสดง Modal
        function showCategoryModal() {
            const modal = new bootstrap.Modal(document.getElementById('categoryModal'));
            modal.show();
        }

        async function loadCategories(showDeleted = false) {
            try {
                const response = await fetch(`/Product/GetCategories?showDeleted=${showDeleted}`);
                const data = await response.json();

                const tbody = document.querySelector('#categoryTable tbody');
                tbody.innerHTML = ''; // เคลียร์ข้อมูลเดิม

                data.forEach(category => {
                    const tr = document.createElement('tr');
                    tr.dataset.categoryId = category.id;
                    tr.dataset.active = !showDeleted;

                    tr.innerHTML = `
                                    <td>
                                        <span class="category-name">${category.name}</span>
                                        <input type="text" class="form-control category-edit d-none" value="${category.name}">
                                    </td>
                                    <td class="text-center">
                                        ${category.productCount} รายการ
                                    </td>
                                    <td class="text-end">
                                        ${!showDeleted ? `
                                            <button class="btn btn-sm btn-warning edit-btn" onclick="startEditCategory(this, ${category.id})">
                                                ✏️ แก้ไข
                                            </button>
                                            <button class="btn btn-sm btn-success save-btn d-none" onclick="saveCategory(this, ${category.id})">
                                                💾 บันทึก
                                            </button>
                                            <button class="btn btn-sm btn-danger" onclick="deleteCategory(${category.id})">
                                                🗑️ ลบ
                                            </button>
                                        ` : `
                                            <button class="btn btn-sm btn-primary" onclick="restoreCategory(${category.id})">
                                                ♻️ กู้คืน
                                            </button>
                                        `}
                                    </td>
                                `;

                    tbody.appendChild(tr);
                });
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการโหลดข้อมูลหมวดหมู่');
            }
        }
        async function addCategory() {
            const name = document.getElementById('newCategoryName').value;
            if (!name) {
                alert('กรุณากรอกชื่อหมวดหมู่');
                return;
            }

            try {
                const response = await fetch('/Product/AddCategory', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ name })
                });

                const result = await response.json();

                if (result.success) {
                    location.reload();
                } else {
                    alert('เกิดข้อผิดพลาด: ' + result.message);
                }
            } catch (error) {
                alert('เกิดข้อผิดพลาดในการเพิ่มหมวดหมู่');
            }
        }

        async function startEditCategory(btn, id) {
            const row = btn.closest('tr');
            row.querySelector('.category-name').classList.add('d-none');
            row.querySelector('.category-edit').classList.remove('d-none');
            row.querySelector('.edit-btn').classList.add('d-none');
            row.querySelector('.save-btn').classList.remove('d-none');
        }

        async function saveCategory(btn, id) {
            const row = btn.closest('tr');
            const input = row.querySelector('.category-edit');
            const name = input.value.trim();

            if (!name) {
                alert('กรุณากรอกชื่อหมวดหมู่');
                input.focus();
                return;
            }

            try {
                const response = await fetch('/Product/UpdateCategory', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ id, name })
                });

                const result = await response.json();

                if (result.success) {
                    // อัพเดตชื่อที่แสดงในตาราง
                    const nameSpan = row.querySelector('.category-name');
                    nameSpan.textContent = name;

                    // ซ่อนฟอร์มแก้ไข แสดงชื่อ
                    nameSpan.classList.remove('d-none');
                    input.classList.add('d-none');

                    // สลับปุ่ม
                    row.querySelector('.edit-btn').classList.remove('d-none');
                    row.querySelector('.save-btn').classList.add('d-none');

                    // อัพเดต dropdown ในฟอร์มสินค้า (ถ้ามี)
                    const categorySelect = document.getElementById('categoryId');
                    if (categorySelect) {
                        const option = categorySelect.querySelector(`option[value="${id}"]`);
                        if (option) {
                            option.textContent = name;
                        }
                    }

                    // อัพเดต dropdown filter (ถ้ามี)
                    const filterSelect = document.getElementById('categoryFilter');
                    if (filterSelect) {
                        const filterOption = filterSelect.querySelector(`option[value="${id}"]`);
                        if (filterOption) {
                            filterOption.textContent = name;
                        }
                    }

                    // แสดงข้อความสำเร็จ
                    const toast = document.createElement('div');
                    toast.className = 'alert alert-success alert-dismissible fade show position-fixed top-0 end-0 m-3';
                    toast.innerHTML = `
                                                                                    บันทึกการแก้ไขเรียบร้อย
                                                                                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                                                                `;
                    document.body.appendChild(toast);

                    // ลบ toast หลังจาก 3 วินาที
                    setTimeout(() => {
                        toast.remove();
                    }, 3000);

                } else {
                    alert('เกิดข้อผิดพลาด: ' + result.message);
                    input.focus();
                }
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการบันทึกข้อมูล');
                input.focus();
            }
        }

        async function deleteCategory(id) {
            if (!confirm('ต้องการลบหมวดหมู่นี้?')) {
                return;
            }

            try {
                const response = await fetch('/Product/DeleteCategory', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(id)  // ส่ง id เป็น JSON
                });

                const result = await response.json();

                if (result.success) {
                    // อัพเดตการแสดงผลในตาราง
                    const row = document.querySelector(`tr[data-category-id="${id}"]`);
                    if (row) {
                        // ซ่อนแถวที่ถูกลบ
                        row.style.display = 'none';
                        // หรือลบแถวออกไปเลย
                        // row.remove();
                    }
                    // แสดงข้อความสำเร็จ
                    alert('ลบหมวดหมู่เรียบร้อยแล้ว');
                } else {
                    alert('เกิดข้อผิดพลาด: ' + result.message);
                }
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการลบหมวดหมู่');
            }
        }
        async function showDeletedCategories() {
            try {
                const response = await fetch('/Product/DeletedCategories');
                const content = await response.text();

                // แสดงหน้าต่าง Modal ใหม่สำหรับแสดงหมวดหมู่ที่ถูกลบ
                const modal = new bootstrap.Modal(document.createElement('div'));
                modal.element.innerHTML = content;
                modal.show();
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการโหลดหมวดหมู่ที่ถูกลบ');
            }
        }

        async function restoreCategory(id) {
            if (!confirm('ต้องการกู้คืนหมวดหมู่นี้?')) {
                return;
            }

            try {
                const response = await fetch('/Product/RestoreCategory', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(id)
                });

                const result = await response.json();

                if (result.success) {
                    // โหลดข้อมูลใหม่
                    const showDeleted = document.getElementById('showDeletedSwitch').checked;
                    loadCategories(showDeleted);
                    alert('กู้คืนหมวดหมู่เรียบร้อยแล้ว');
                } else {
                    alert('เกิดข้อผิดพลาด: ' + result.message);
                }
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการกู้คืนหมวดหมู่');
            }
        }
    </script>

    @* //script product *@
    <script>
        let productModal;
        let categorySelect; // เพิ่มตัวแปรสำหรับเก็บ reference ของ select

        document.addEventListener('DOMContentLoaded', function () {
            productModal = new bootstrap.Modal(document.getElementById('productModal'));
            categorySelect = document.getElementById('categoryId'); // select สำหรับหมวดหมู่ในฟอร์มสินค้า
            loadCategoriesForProduct(); // โหลดหมวดหมู่ครั้งแรก
        });

        document.getElementById('image').addEventListener('change', function (e) {
            const preview = document.getElementById('imagePreview');
            preview.innerHTML = '';

            if (this.files && this.files[0]) {
                const reader = new FileReader();
                reader.onload = function (e) {
                    const img = document.createElement('img');
                    img.src = e.target.result;
                    img.classList.add('img-fluid', 'mt-2', 'rounded');
                    img.style.maxHeight = '200px';
                    preview.appendChild(img);
                }
                reader.readAsDataURL(this.files[0]);
            }
        });

        async function loadCategoriesForProduct() {
            try {
                const response = await fetch('/Product/GetActiveCategories'); // เพิ่ม endpoint ใหม่
                const categories = await response.json();

                // อัพเดต dropdown ในฟอร์มสินค้า
                if (categorySelect) {
                    categorySelect.innerHTML = ''; // เคลียร์ตัวเลือกเดิม

                    // เพิ่มตัวเลือกเริ่มต้น
                    const defaultOption = document.createElement('option');
                    defaultOption.value = '';
                    defaultOption.textContent = '-- เลือกหมวดหมู่ --';
                    categorySelect.appendChild(defaultOption);

                    // เพิ่มรายการหมวดหมู่
                    categories.forEach(category => {
                        const option = document.createElement('option');
                        option.value = category.id;
                        option.textContent = category.name;
                        categorySelect.appendChild(option);
                    });
                }
            } catch (error) {
                console.error('Error loading categories:', error);
                alert('เกิดข้อผิดพลาดในการโหลดข้อมูลหมวดหมู่');
            }
        }

               

        function showAddModal() {
            loadCategoriesForProduct(); // โหลดหมวดหมู่ใหม่ก่อนแสดง modal
            document.getElementById('modalTitle').textContent = 'เพิ่มสินค้า';
            document.getElementById('productForm').reset();
            document.getElementById('productId').value = '';
            productModal.show();
        }

        function showAddModal() {
            loadCategoriesForProduct(); // โหลดหมวดหมู่ใหม่ก่อนแสดง modal
            document.getElementById('modalTitle').textContent = 'เพิ่มสินค้า';
            document.getElementById('productForm').reset();
            document.getElementById('productId').value = '';
            productModal.show();
        }

        async function toggleStatus(id, checkbox) {
            try {
                const response = await fetch('/Product/ToggleStatus', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(id)  // ส่งแค่ id อย่างเดียว
                });

                const result = await response.json();
                
                if (result.success) {
                    const status = result.newStatus;
                    const toast = document.createElement('div');
                    toast.className = `alert alert-${status ? 'success' : 'warning'} alert-dismissible fade show position-fixed top-0 end-0 m-3`;
                    toast.innerHTML = `
                        ${result.message}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    `;
                    document.body.appendChild(toast);
                    
                    setTimeout(() => {
                        toast.remove();
                    }, 3000);
                } else {
                    alert('เกิดข้อผิดพลาด: ' + result.message);
                    checkbox.checked = !checkbox.checked;  // Reset checkbox state
                }
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการเปลี่ยนสถานะสินค้า');
                checkbox.checked = !checkbox.checked;  // Reset checkbox state
            }
        }
        // AddCategory
        async function addCategory() {
            const nameInput = document.getElementById('newCategoryName');
            const name = nameInput.value.trim();

            if (!name) {
                alert('กรุณากรอกชื่อหมวดหมู่');
                nameInput.focus();
                return;
            }

            try {
                const response = await fetch('/Product/AddCategory', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ name })
                });

                const result = await response.json();

                if (result.success) {
                    // เคลียร์ค่าใน input
                    nameInput.value = '';
                    nameInput.focus();

                    // อัพเดตตารางหมวดหมู่โดยไม่ต้องรีโหลดหน้า
                    const tbody = document.querySelector('#categoryTable tbody');
                    const newRow = document.createElement('tr');
                    newRow.innerHTML = `
                            <td>
                                <span class="category-name">${result.category.name}</span>
                                <input type="text" class="form-control category-edit d-none" value="${result.category.name}">
                            </td>
                            <td>0 รายการ</td>
                            <td class="text-end">
                                <button class="btn btn-sm btn-warning edit-btn" onclick="startEditCategory(this, ${result.category.id})">
                                    ✏️ แก้ไข
                                </button>
                                <button class="btn btn-sm btn-success save-btn d-none" onclick="saveCategory(this, ${result.category.id})">
                                    💾 บันทึก
                                </button>
                                <button class="btn btn-sm btn-danger" onclick="deleteCategory(${result.category.id})">
                                    🗑️ ลบ
                                </button>
                            </td>
                                `;
                    tbody.appendChild(newRow);

                    // อัพเดต dropdown ในฟอร์มสินค้า
                    const categorySelect = document.getElementById('categoryId');
                    const option = document.createElement('option');
                    option.value = result.category.id;
                    option.textContent = result.category.name;
                    categorySelect.appendChild(option);

                    // อัพเดต dropdown filter
                    const filterSelect = document.getElementById('categoryFilter');
                    const filterOption = document.createElement('option');
                    filterOption.value = result.category.id;
                    filterOption.textContent = result.category.name;
                    filterSelect.appendChild(filterOption);

                } else {
                    alert('เกิดข้อผิดพลาด: ' + result.message);
                    nameInput.focus();
                }
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการเพิ่มหมวดหมู่');
            }
        }
        async function saveCategory(btn, id) {
            const row = btn.closest('tr');
            const input = row.querySelector('.category-edit');
            const name = input.value.trim();

            if (!name) {
                alert('กรุณากรอกชื่อหมวดหมู่');
                input.focus();
                return;
            }

            try {
                const response = await fetch('/Product/UpdateCategory', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ id, name })
                });

                const result = await response.json();

                if (result.success) {
                    // โหลดข้อมูลใหม่
                    const showDeleted = document.getElementById('showDeletedSwitch').checked;
                    loadCategories(showDeleted);

                    // แสดงข้อความแจ้งเตือน
                    alert('บันทึกการแก้ไขเรียบร้อย');
                } else {
                    alert('เกิดข้อผิดพลาด: ' + result.message);
                    input.focus();
                }
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการบันทึกข้อมูล');
                input.focus();
            }
        }

        async function deleteCategory(id) {
            if (!confirm('ต้องการลบหมวดหมู่นี้?')) {
                return;
            }

            try {
                const response = await fetch(`/Product/DeleteCategory?id=${id}`, {  // แก้ไขการส่ง id
                    method: 'POST'
                });

                const result = await response.json();

                if (result.success) {
                    // อัพเดตการแสดงผลในตาราง
                    const row = document.querySelector(`tr[data-category-id="${id}"]`);
                    if (row) {
                        row.style.display = 'none';
                    }
                    alert('ลบหมวดหมู่เรียบร้อยแล้ว');

                    // โหลดข้อมูลใหม่
                    const showDeleted = document.getElementById('showDeletedSwitch').checked;
                    loadCategories(showDeleted);
                } else {
                    alert('เกิดข้อผิดพลาด: ' + result.message);
                }
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการลบหมวดหมู่');
            }
        }
        // เพิ่มการตรวจสอบการกด Enter ที่ช่องเพิ่มหมวดหมู่
        document.getElementById('newCategoryName').addEventListener('keypress', function (e) {
            if (e.key === 'Enter') {
                e.preventDefault();
                addCategory();
            }
        });

        // AddProduct
        async function saveProduct() {
            const form = document.getElementById('productForm');
            if (!form.checkValidity()) {
                form.reportValidity();
                return;
            }

            try {
                const formData = new FormData();
                const id = document.getElementById('productId').value;
                
                // เพิ่ม Id เข้าไปใน formData
                formData.append('Id', id);  // เพิ่มบรรทัดนี้
                formData.append('Name', document.getElementById('name').value.trim());
                formData.append('Barcode', document.getElementById('barcode').value.trim());
                formData.append('CategoryId', document.getElementById('categoryId').value);
                formData.append('Price', document.getElementById('price').value);
                formData.append('Stock', document.getElementById('stock').value);

                const imageFile = document.getElementById('image').files[0];
                if (imageFile) {
                    formData.append('imageFile', imageFile);
                }

                // แก้ไข URL
                const url = id ? '/Product/Update' : '/Product/Create';
                
                const response = await fetch(url, {
                    method: 'POST',
                    body: formData
                });

                const result = await response.json();
                
                if (result.success) {
                    alert(result.message || 'บันทึกข้อมูลสำเร็จ');
                    location.reload();
                } else {
                    alert(result.message || 'เกิดข้อผิดพลาดในการบันทึกข้อมูล');
                }
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการบันทึกข้อมูล');
            }
        }        
        async function showEditModal(id) {
            try {
                // แก้ URL ให้ตรงกับ Route
                const response = await fetch(`/Product/GetProduct/${id}`);
                const result = await response.json();

                if (!result.success) {
                    alert(result.message);
                    return;
                }

                const product = result.data;

                // กำหนดค่าให้ฟอร์ม
                document.getElementById('productId').value = product.id;
                document.getElementById('categoryId').value = product.categoryId;
                document.getElementById('name').value = product.name;
                document.getElementById('barcode').value = product.barcode;
                document.getElementById('price').value = product.price;
                document.getElementById('stock').value = product.stock;

                // แสดงรูปภาพถ้ามี
                const imagePreview = document.getElementById('imagePreview');
                imagePreview.innerHTML = '';
                if (product.imageBase64) {
                    const img = document.createElement('img');
                    img.src = `data:${product.imageType};base64,${product.imageBase64}`;
                    img.classList.add('img-fluid', 'mt-2', 'rounded');
                    img.style.maxHeight = '200px';
                    imagePreview.appendChild(img);
                }

                // เปลี่ยนชื่อ Modal
                document.getElementById('modalTitle').textContent = 'แก้ไขสินค้า';
                
                // เปิด Modal
                productModal.show();

            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการโหลดข้อมูลสินค้า');
            }
        }        
        function confirmDelete(id) {
            if (confirm('ยืนยันการลบสินค้า?')) {
                deleteProduct(id);
            }
        }

        async function deleteProduct(id) {
            if (!confirm('ต้องการลบสินค้านี้?')) {
                return;
            }

            try {
                const response = await fetch('/Product/Delete', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(id)  // ส่งแค่ id อย่างเดียว
                });

                const result = await response.json();
                
                if (result.success) {
                    alert(result.message);
                    location.reload();
                } else {
                    alert('เกิดข้อผิดพลาด: ' + result.message);
                }
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการลบสินค้า');
            }
        }

        function filterProducts() {
            const search = document.getElementById('searchInput').value.toLowerCase();
            const categoryId = document.getElementById('categoryFilter').value;
            const rows = document.querySelectorAll('tbody tr');

            rows.forEach(row => {
                // แก้ไขตำแหน่งคอลัมน์ให้ตรงกับ HTML
                const categoryCell = row.cells[0];  // ชื่อหมวดอยู่คอลัมน์แรก
                const barcode = row.cells[1].textContent.toLowerCase();  // บาร์โค้ดอยู่คอลัมน์ที่ 2
                const name = row.cells[2].textContent.toLowerCase();    // ชื่อสินค้าอยู่คอลัมน์ที่ 3

                // ตรวจสอบการกรองตามหมวดหมู่
                const categoryMatch = !categoryId || categoryCell.getAttribute('data-category-id') === categoryId;

                // ค้นหาทั้งชื่อและบาร์โค้ด
                const searchMatch = name.includes(search) || barcode.includes(search);

                // แสดง/ซ่อนแถว
                row.style.display = (searchMatch && categoryMatch) ? '' : 'none';

                // Debug
                console.log('Row:', {
                    categoryId: categoryCell.getAttribute('data-category-id'),
                    selectedCategory: categoryId,
                    categoryMatch,
                    searchMatch,
                    visible: row.style.display === ''
                });
            });

            // แสดงจำนวนรายการที่พบ
            const visibleRows = document.querySelectorAll('tbody tr:not([style*="display: none"])').length;
            const totalRows = rows.length;
            
            // อัพเดตข้อความแสดงจำนวนรายการ
            const resultCount = document.getElementById('resultCount');
            if (resultCount) {
                resultCount.textContent = `แสดง ${visibleRows} จาก ${totalRows} รายการ`;
            }
        }        
        
        // เพิ่ม Event Listener
        document.addEventListener('DOMContentLoaded', function() {
            // เพิ่ม event listener สำหรับ input ค้นหา
            const searchInput = document.getElementById('searchInput');
            if (searchInput) {
                searchInput.addEventListener('input', filterProducts);
            }

            // เพิ่ม event listener สำหรับ dropdown หมวดหมู่
            const categoryFilter = document.getElementById('categoryFilter');
            if (categoryFilter) {
                categoryFilter.addEventListener('change', filterProducts);
            }

            // เรียกฟังก์ชันครั้งแรกเพื่อแสดงจำนวนรายการ
            filterProducts();
        });

        async function exportTemplate() {
    window.location.href = '/Product/ExportTemplate';
}

        async function importExcel(input) {
            if (!input.files || input.files.length === 0) {
                alert('กรุณาเลือกไฟล์');
                return;
            }

            const file = input.files[0];
            if (!file.name.endsWith('.xlsx')) {
                alert('กรุณาเลือกไฟล์ Excel (.xlsx) เท่านั้น');
                input.value = '';
                return;
            }

            const formData = new FormData();
            formData.append('file', file);

            try {
                const response = await fetch('/Product/ImportExcel', {
                    method: 'POST',
                    body: formData
                });

                const result = await response.json();
                
                if (result.success) {
                    alert(result.message);
                    location.reload();
                } else {
                    alert('เกิดข้อผิดพลาด: ' + result.message);
                }
            } catch (error) {
                console.error('Error:', error);
                alert('เกิดข้อผิดพลาดในการนำเข้าข้อมูล');
            }

            // เคลียร์ไฟล์ที่เลือก
            input.value = '';
        }

    </script>
}